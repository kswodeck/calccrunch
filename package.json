{
  "name": "calccrunch",
  "type": "module",
  "version": "1.0.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "npm run update-counts && npm run gen:icons && astro dev",
    "build": "npm run clean && npm run validate:content && npm run update-counts && npm run gen:icons && astro build && npm run check:links",
    "preview": "astro preview",
    "analyze": "npm run build && npx lighthouse https://calccrunch.com --output html --view",
    "update-counts": "node src/scripts/update-category-counts.mjs",
    "validate:content": "node scripts/validate-content.mjs",
    "check:links": "node scripts/check-links.mjs",
    "gen:icons": "node scripts/generate-icons.mjs",
    "gen:conversions": "node scripts/generate-conversions.mjs",
    "new:calc": "node scripts/new-calculator.mjs",
    "new:post": "node scripts/new-post.mjs",
    "astro": "astro",
    "clean": "rm -rf dist .astro node_modules/.vite",
    "check": "astro check --no-types"
  },
  "dependencies": {
    "@astrojs/netlify": "^7.0.9",
    "astro": "^6.3.3",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.2"
  }
}
